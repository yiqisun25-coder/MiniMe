<view class="page-container">
  <!-- 顶部日期 -->
  <view class="top-meta">
    <text class="top-date">{{heroDate}}</text>
    <text class="page-title">陪陪我</text>
  </view>

  <!-- 头像区域 -->
  <view class="avatar-section">
    <view class="avatar-halo"></view>
    <view class="avatar-ring" bindtap="onAvatarTap">
      <image class="hero-avatar" src="{{avatarSrc}}" mode="aspectFill" />
    </view>
    <text wx:if="{{adminHint}}" class="admin-hint">{{adminHint}}</text>
    <!-- 侧边图标 -->
    <view class="side-icon side-icon-left">
      <text>☀️</text>
      <text wx:if="{{isDaughter}}" class="side-icon-label">拍拍我</text>
    </view>
    <view class="side-icon side-icon-right">
      <text>🌙</text>
      <text wx:if="{{isDaughter}}" class="side-icon-label">拍拍我</text>
    </view>
    <!-- 状态气泡：点场景后显示回应，默认显示固定文字 -->
    <view class="status-bubble {{bubbleVisible ? 'bubble-show' : ''}}">
      <text class="status-text">{{bubbleVisible ? currentResponse : '「妈～ 我在认真上班哦 💪」'}}</text>
    </view>
  </view>

  <!-- 动作网格：女儿端 = 功能入口，妈妈端 = 场景卡片 -->
  <view class="action-section">
    <text class="action-title">{{isDaughter ? '今天想做什么？' : '想和女儿说点什么？'}}</text>

    <!-- 女儿端功能网格 -->
    <view wx:if="{{isDaughter}}" class="action-grid">
      <view class="action-card morning" bindtap="goCompose">
        <text class="action-emoji">✉️</text>
        <text class="action-label">给妈妈写信</text>
        <text class="action-sub">写封长信</text>
      </view>
      <view class="action-card night" bindtap="goDaily">
        <text class="action-emoji">📝</text>
        <text class="action-label">存我的日常</text>
        <text class="action-sub">记录今天</text>
      </view>
      <view class="action-card miss" bindtap="goMemory">
        <text class="action-emoji">🔒</text>
        <text class="action-label">预存记忆</text>
        <text class="action-sub">定时解锁</text>
      </view>
      <view class="action-card secret" bindtap="goAdmin">
        <text class="action-emoji">⚙️</text>
        <text class="action-label">点我设置</text>
        <text class="action-sub">管理家庭</text>
      </view>
    </view>

    <!-- 妈妈端场景网格 -->
    <view wx:else class="action-grid">
      <view
        wx:for="{{scenes}}"
        wx:key="key"
        class="action-card {{item.key}}"
        data-key="{{item.key}}"
        bindtap="onSceneTap"
      >
        <text class="action-emoji">{{item.emoji}}</text>
        <text class="action-label">{{item.label}}</text>
        <text class="action-sub">{{item.desc}}</text>
      </view>
    </view>
  </view>

  <!-- 收信箱入口 -->
  <view class="letter-entry" bindtap="goToMailbox">
    <view class="letter-entry-left">
      <text class="letter-entry-icon">💌</text>
      <view>
        <text class="letter-entry-title">给你的信</text>
        <text class="letter-entry-sub">妈妈的收信箱，有新消息会提醒你</text>
      </view>
    </view>
    <view class="letter-entry-right">
      <view class="letter-badge" wx:if="{{unreadCount > 0}}">
        <text class="letter-badge-num">{{unreadCount}}</text>
      </view>
      <text class="letter-entry-arrow">›</text>
    </view>
  </view>
</view>
